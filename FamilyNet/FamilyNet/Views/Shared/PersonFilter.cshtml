@model IEnumerable<FamilyNet.Infrastructure.FilterUtility.FilterParams>
@{
    Layout = null;
}

<form asp-action="Filter">
    <table class="table">
        @foreach (var item in Model)
        {
        <th>
            @Html.Label(@Html.DisplayNameFor(modelItem => item.ColumnName) + ":")
            @Html.TextBox(item.FilterValue, "", new { @class = "form-control" })
        </th>
        }

        @*<th>
            @Html.Label(@Html.DisplayNameFor(model => model.ToString()) + ":")
            @Html.TextBox("FullNameString", "", new { @class = "form-control" })
        </th>
        <th width="11%">
            @Html.Label(@Html.DisplayNameFor(model => model.ToString()) + " от:")
            @Html.TextBox("RatingNumber", "", new { @class = "form-control", type = "number" })
        </th>*@
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th>
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-filter"></i>
            </button>
            @*TODO: Work with Person (maybe null contr) <а asp-action="Index"></a> *@
            @Html.ActionLink("", "Index", "Representatives", null, new { @class = "fas fa-ban" })
        </th>

    </table>
</form>